# Mission GroundWork redeploy handoff

## Current state

The public website URL previously associated with this repository returns `404 DEPLOYMENT_NOT_FOUND`.

## Canonical source

- Repository: `mozartg/missiongroundwork`
- Branch: `main`
- Source archive: `https://github.com/mozartg/missiongroundwork/archive/refs/heads/main.zip`
- Build command: `npm run build`
- Output directory: `dist`

## Owner action

Download the source archive and re-upload or import it into the selected static hosting provider.

## Verification required after upload

1. The public URL returns HTTP 200.
2. The page title and visible content identify Mission GroundWork.
3. Navigation and primary calls to action render correctly on mobile and desktop.
4. The repository website field is updated to the verified URL.
5. The old dead Vercel URL is removed or replaced.

A repository upload or hosting configuration is not completion until the resulting public page is opened and inspected.