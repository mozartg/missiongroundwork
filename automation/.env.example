# Copy to .env; never commit the populated file.
N8N_ENCRYPTION_KEY=replace-with-a-long-random-string
PLUNK_DB_PASSWORD=replace-with-a-long-random-password

# Add only when the related workflow is enabled.
GOOGLE_SHEETS_SPREADSHEET_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
BASEROW_API_URL=http://localhost:8000
BASEROW_API_TOKEN=
SUMMARY_EMAIL=

# Optional model providers. Keep blank to disable.
OPENAI_API_KEY=
GEMINI_API_KEY=
